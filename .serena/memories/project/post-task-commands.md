# Post-Task Commands

After completing any code changes, run the following commands to ensure code quality:

```bash
npm run lint    # Run ESLint to check for linting errors
```

## Notes
- The project uses Next.js built-in linting with ESLint
- No TypeScript type checking command is explicitly configured (likely integrated with build/lint)
- Always test changes in the development server before committing